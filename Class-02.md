# 1-HTML has six "levels" of headings:<h1> to <h6>.
# 2-<h1> element is the largest, and the contents of an <h6> element is the smallest. 
# 3-To create paragraph you can use <p> element .
# 4-You can use <b> element to represents section to be bold. 
# 5-You can use <I> element to represents section to be italic. 
# 7-The <sub> element is used to contain characters that should be subscript.
# 8-The <sup> element is used to contain characters that should be superscript.
# 9-When the browser comes across two or more spaces next to each other, it only displays one space. 
# 10-You wanted to add a line break inside the middle of a paragraph you can use the line break tag </br>.
# 11-You can add a horizontal rule between sections using the <hr /> tag. 
# 12-The use of the <strong> element indicates that its content has strong importance , and the contents of element will be bold .
# 13-The <em> element contents will be italic.
# 14-The <blockquote> element is used for longer quotes that take up an entire paragraph. 
# 15-The <q> element is used for shorter quotes that sit within a paragraph. 
# 16-<abbr> element used to specify the full term.
# 17-The <dfn> element is used to indicate the defining instance of a new term. 
# 18-The content of a <ins> element is usually underlined, while the content of a <del> element usually has a line through it. 
# 19-By using CSS you can add color , background color and border .
# 20-A CSS rule contains two parts: a selector and a declaration. 
# 21-CSS declarations sit inside curly brackets and each is made up of two parts: a property and a value .
# 22-If the two selectors are identical, the latter of the two will take precedence. 
# 23-ExternaL styLe sheet better to use , because all of your web pages can share the same style sheet. 
# 24-When you need to write a comment in JavaScript start with “/“ .
# 25-Comment help to explain the code .
# 26-Type of variable (Sting , Numeric , boolean).
# 27-You can store variable to store number and sting .
# 28-The name must begin with a letter, dollar sign ($),or an underscore (_). It must not start with a number. 
# 29-An array is a special type of variable. It doesn't just store one value; it stores a list of values. 
# 30-The values in the array do not need to be the same data type, so you can store a string, a number and a Boolean all in the same array. 
# 31-Values in an array are accessed as if they are in a numbered list. It is important to know that the numbering of this list starts at zero (not one). 
# 32-Javascript contain mathematical operators with numbers(+, -, *, ++,—)
# 33-You can use mathematical operators with string is (+).
# 34-You can evaluate a situation by comparing one value in the. Script to. What you expect it , the result will be boolean (true or false ).
# 35- Their are many operators in java like (== , != , === ,!== , < , > ,>= , <=).
# 36-Comparison operators usually return single values of true and false .
# 37-Logical operators allow to you compare more than one comparison operator.
# 38-The if statement checks a condition if the condition goes true will run the code in side curly brace.
# 39- The if else statement checks a condition , if it resolves to rue the first code block is executed ,if the condition resolves to false the.  Second code block is run instead .
